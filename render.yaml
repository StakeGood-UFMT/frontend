services:
  - type: static
    name: stakegood-frontend
    env: static
    plan: free
    buildCommand: npm install && npm run build
    publishPath: dist/stake-good-frontend/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
